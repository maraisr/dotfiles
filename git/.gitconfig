[alias]
	st = status -sb
	co = checkout
	br = branch
	ft = fetch -aupt
	save = "!git add . && git commit -m ':construction: WIP' -n"
	cleangone = "!git branch -vv | grep ': gone]' | awk '{print $1}' | xargs git branch -D"
	cleanup = "!git branch | grep -v 'master' | xargs git branch -D; git tag -l | xargs git tag -d; git ft -t"
	cm = commit -m
[user]
	name = Marais Rossouw
	email = hi@marais.io
	signingkey = 2FACFD47AF4D59A4
[core]
	autocrlf = input
	eol = ef
	editor = vim
	ignorecase = false
[rerere]
	enabled = true
[log]
	date = relative
[format]
	pretty = format:%C(auto,yellow)%h%C(auto,magenta)% G? %C(auto,blue)%>(12,trunc)%ad %C(auto,green)%<(7,trunc)%aN%C(auto,reset)%s%C(auto,red)% gD% D
[difftool]
	prompt = false
[pull]
	rebase = true
[push]
	default = current
